<!DOCTYPE html>
<html>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  <head>
    <%= favicon_link_tag 'icon.ico' %>
    <title>からくり映像評論家</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark base-bg-color">
      <div class="container">
        <% if user_signed_in? %><!-- ユーザーがログインしていた時の処理 -->
        <a class="navbar-brand deco-font light-color shadow" href="/videos" style="font-size:1.4rem;">からくり映像評論家</a>
        <% else %><!-- ユーザーがログインしていなかった時の処理 -->
        <a class="navbar-brand deco-font light-color shadow" href="/" style="font-size:1.4rem;">からくり映像評論家</a>
        <% end %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %><!-- ユーザーがログインしていた時の処理 -->
            <li><%= link_to "マイページ", videos_path, class: "nav-link btn btn-sm btn-light deco-font light-color" %></li>
            <li><%= link_to "アップロード", new_video_path, class: "nav-link btn btn-sm btn-light deco-font light-color" %></li>
            <li><%= link_to "ランキング", ranks_path , class: "nav-link btn btn-sm btn-light deco-font light-color" %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, class: "nav-link btn btn-sm btn-light deco-font light-color" %></li>

          <% else %><!-- ユーザーがログインしていなかった時の処理 -->
            <li><%= link_to "ログイン", new_user_session_path , class: "post nav-link btn btn-sm btn-light deco-font light-color"%></li><!-- ログイン画面に移行する -->
            <li><%= link_to "新規登録", new_user_registration_path , class: "post nav-link btn btn-sm btn-light deco-font light-color"%></li><!-- 新規登録画面に移行する -->
          <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <body class="light-bg-color">
    <%= yield %>
  </body>
  
</html>
